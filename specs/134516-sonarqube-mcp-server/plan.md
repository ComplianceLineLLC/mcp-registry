# Deploy SonarQube MCP Server to Azure (Container Apps) + Monitoring

> Tracked by NWOW work item **#134516 - Add SonarQube MCP Server**. Location/naming here loosely follows the `specs/<work-item-id>-<slug>/` convention seen elsewhere in the org (e.g. `policy-management`), without adopting the full GitHub spec-kit tool scaffolding (`.specify/`, `constitution.md`, etc.) — just the folder convention for discoverability.

## Context

Ethico wants engineers to use the SonarQube MCP through GitHub Copilot without installing Docker locally. The registry (this repo) already documents the intended architecture from prior research (commit `5bc588d`): a centrally hosted, internal-only Azure Container App that developers reach over VPN, using per-developer `Authorization: Bearer <token>` — see [docs/sonarqube-deployment.md](/docs/sonarqube-deployment.md) and the "SonarQube" section of [README.md](/README.md).

This work is tracked by NWOW work item **#134516 - Add SonarQube MCP Server** (time/7Pace), with the actual work tracked via a GitHub issue in this repo — continuing the pattern used for Figma (#100587) and Angular/MS Learn (#98850).

### Decisions confirmed so far

- **SonarQube Server** (public, reachable from anywhere): `https://sqdev.mycompliancemanagement.com/` — no new network peering needed for outbound reachability.
- **Target subscription**: confirmed — **Azure DevTest subscription 1** (`326c5c7f-73c8-4e8b-b146-d643c06ced0d`, tenant `complianceline.com` / `b3f8ea1b-fcb5-4178-b14c-7625dd8865ca`).
- **Image supply chain**: pull `sonarsource/sonarqube-mcp` from Docker Hub, Trivy-scan it (per the existing policy in [docs/mcp-maintenance.md](/docs/mcp-maintenance.md)), then push the vetted image into a new **Azure Container Registry (ACR)** we control, pinned by digest. The Container App pulls from ACR, not Docker Hub directly — gives an audit trail and isolates us from upstream tag mutation/outages.
- **Update mechanism**: an **Azure DevOps Pipeline** owns the "pull → scan → promote to ACR → deploy → verify" flow, replacing the manual `az containerapp update` steps currently written in the maintenance doc. Needs an Azure Resource Manager service connection from ADO into the target subscription (scoped to the resource group, granting rights to ACR + the Container App) — as the org's Azure DevOps organization owner, Walter can create this himself; no separate approval or handoff needed. Note: `devops/Vulnerability_Scan/templates/` only covers .NET (NuGet) and npm dependency scanning today — there is no existing Docker/container-image scan template to reuse, so the Trivy step is new, first-of-its-kind pipeline work, not a drop-in. The deploy step itself should call the existing `devops/Azure/UpdateAzureContainer_WithEnv.ps1` (diffs an `appsettings_envmapping.json` against the live revision's env vars, then runs `az containerapp update --image ... --revision-suffix ... --replace-env-vars`) rather than hand-rolling new CLI calls — the build side of the pipeline needs to emit an `appsettings_envmapping.json` artifact for the SonarQube MCP's env vars (no KeyVault secrets needed; there's no shared token).
  - **Correction (2026-08-11): both the Bicep and the pipeline YAML live in *this* repo, not `devops`.** The original plan assumed a `devops/Pipelines/<Capability>` + `devops/infrastructure/<Capability>` convention citing `devops/CLAUDE.md` — that file doesn't exist on `devops`'s `Develop` branch. It does exist on Walter's own unmerged branch (`wbates/126802-Add-docs-for-pipeline-requirements`), and it says the opposite: `devops` is explicitly carved out as shared-templates-only ("hosts shared Azure DevOps pipeline tooling... does not build application code itself"); each app owns its own `/Pipelines` in its own repo. Since `mcp-registry` isn't a single app but a registry of many MCP servers, new decision: an app-scoped subfolder per hosted MCP to avoid mixing — Bicep now lives at [infrastructure/SonarQubeMCP/](../../infrastructure/SonarQubeMCP/) (already authored: `main.bicep` at subscription scope + `modules/` for the ACR, a user-assigned managed identity, and its `AcrPull` role assignment), and the pipeline YAML (task #7) will follow the same pattern at `Pipelines/SonarQubeMCP/`. `devops` stays referenced only for genuinely shared pieces: `Azure/UpdateAzureContainer_WithEnv.ps1` and possibly a new reusable Trivy template under `Vulnerability_Scan/templates/` if that becomes the shared pattern for other repos too.
  - Also decided: the Container App's identity is a **user-assigned** managed identity (`ethico-sonarqube-mcp-mi-dev`, provisioned in task #3's Bicep), not system-assigned — chosen for auditability and so its `AcrPull` grant survives if the Container App itself is ever recreated, at the cost of one extra resource to manage. Deliberately **not** using ACR's newer Entra ABAC repository-scoped permissions (confirmed real and GA via Microsoft Learn, not preview) — reusing an existing shared ACR (`ethicodev` in the Pay-As-You-Go Dev/Test subscription, which already holds 6 unrelated repos with zero existing scope-map isolation) would need a disruptive registry-wide migration affecting other teams' live workloads, and ABAC on a single-purpose new registry has no one else to isolate from.
- **Developer authentication**: already the right design — the Container App is stateless and holds no shared SonarQube token. Each developer generates their **own SonarQube USER token** (via the SonarQube UI: *My Account → Security → Generate Tokens*, using a `mcp-<username>` naming convention so it's distinguishable from other tokens like CI/CD scanner tokens) and adds it to their own local `mcp.json` as `Authorization: Bearer <token>`. This gives per-developer usage tracking for free via SonarQube's own audit/access logs (no shared/global token).
- **Token expiration & rotation**: as SonarQube admin, set a global max token lifetime under *Administration → Configuration → Security* so no MCP token can be created without an expiration date. SonarQube doesn't natively email users before a token expires, so a scheduled Azure DevOps pipeline (also living at `devops/Pipelines/SonarQube/`) will poll the SonarQube Web API (`api/users/search` + `api/user_tokens/search?login=<user>`, using an admin token stored as a pipeline secret) for `mcp-*` tokens nearing expiration and email the owner ahead of time. **Not** the Azure Monitor Action Group used for Container App alerts — that targets a fixed ops distribution list, not individual developers. Instead, adapt the existing pipeline-driven email pattern from `devops/Vulnerability_Scan/sendemail_scan.ps1` (PowerShell + `System.Net.Mail.SmtpClient` against AWS SES's SMTP interface, credentials from a variable group) to send a per-developer reminder instead of a single fixed-recipient report.
- **Monitoring**: Azure Monitor + Action Group email as the baseline. New Relic is used org-wide for dev/prod monitoring — turns out this is **not** actually Ethico's first Azure→New Relic integration as originally assumed: an existing Azure AD app registration (`NewRelic-Integrations`, App ID `eefe04be-8c9b-4247-81c2-d484f1091389`, same AAD tenant `b3f8ea1b-...`) was already linked into New Relic for the UAT subscription (`a52cf3c4-...`). Reused that same app registration for this project rather than registering a new one: generated an additional client secret on it (via Azure Portal), added a new New Relic "Azure account" entry scoped to `326c5c7f-...` / `rg-ethico-sonarqube-mcp-dev` with **Azure Monitor metrics** enabled (not the deprecated "Azure Polling for Functions" method the UAT entry still uses, and not Cost Management — out of scope and would need broader-than-RG Reader scope to be useful). The real schedule risk the plan originally flagged hasn't gone away, just moved: whether Azure Monitor metrics actually flow through this reused app registration won't be provable until the Container App exists and we check New Relic for real data (see task #6 status and the Wed verification step).
- **Rollout**: pilot first (you + 1-2 volunteers) before an org-wide announcement.

### Known bug to fix along the way

The registry files committed in `5bc588d` describe the SonarQube entry as a **stdio** package (`docker run ... -e SONARQUBE_TOKEN -e SONARQUBE_URL`), but the actual architecture is an **HTTP remote** with a per-developer Bearer token — same pattern as the Figma/Microsoft Learn entries (`executionModel: "remote-http"` + `remotes: [...]`). This needs correcting to the real Container App URL, or the registry will tell VS Code to try to run Docker locally instead of connecting to the hosted server.

## Task List & Estimates

| # | Task | Estimate |
|---|---|---|
| 1 | ~~Confirm target subscription~~ — **Done**: Azure DevTest subscription 1 (`326c5c7f-73c8-4e8b-b146-d643c06ced0d`) | 15–30 min |
| 2 | ~~Networking~~ — **Done.** Gilbert approved the proposed subnet/egress path in [network-architecture.html](network-architecture.html); `SonarQubeMCP-Dev-Subnet` (`20.0.3.160/27` in `clDEVvNET`) created and verified — delegated to `Microsoft.App/environments`, joined to `NSG-DEVSubscription` + `DEV-RouteTable` as designed. Captured as IaC at [infrastructure/SonarQubeMCP/modules/networking.bicep](../../infrastructure/SonarQubeMCP/modules/networking.bicep) (deployed manually this time; the module exists for reproducibility, kept separate from `main.bicep` since it touches the shared VNet in `RG-PolicyManagement`, not this workload's own resource group) | ~2 hours |
| 3 | ~~Create the ACR, vet, and promote the image~~ — **Done.** Deployed [infrastructure/SonarQubeMCP/main.bicep](../../infrastructure/SonarQubeMCP/main.bicep) (ACR, managed identity, `AcrPull` role assignment). The originally-planned pin `1.20.0.2929` scanned with 4 unaddressed HIGH findings, so checked GitHub Releases and promoted **`1.24.0.3152`** instead (4 releases ahead, scanned clean) — pinned by digest `sha256:edf80a38…` in ACR. Full history in [RUNBOOK.md](../../infrastructure/SonarQubeMCP/RUNBOOK.md#step-3--image-promotion). **Follow-up:** several docs/registry files are still pinned to `1.20.0.2929` — see task #8 | 0.5 day |
| 4 | Infrastructure as Code: extend [infrastructure/SonarQubeMCP/main.bicep](../../infrastructure/SonarQubeMCP/main.bicep) (not the `devops` repo — see corrected decision above) with the Container Apps Environment (`internal: true`, wired to the subnet from #2), the Container App itself (image from ACR by digest, attach the already-provisioned `ethico-sonarqube-mcp-mi-dev` user-assigned identity — its `AcrPull` grant already exists from task #3, ingress internal, port 8080, env vars per [docs/sonarqube-deployment.md](/docs/sonarqube-deployment.md): `SONARQUBE_TRANSPORT=http`, `SONARQUBE_URL=https://sqdev.mycompliancemanagement.com`, `SONARQUBE_READ_ONLY=true`, `TELEMETRY_DISABLED=true`, `SONARQUBE_MCP_IN_CONTAINER=true`, min/max replicas 1/2), and a Log Analytics workspace + diagnostic settings | 1–1.5 days |
| 5 | Monitoring: Azure Monitor alert rules (via Bicep from #4 where practical) for health-probe failures/replica restarts, CPU/memory thresholds, and error-level log entries; wire an Action Group to email | 0.5 day |
| 6 | New Relic: **front-loadable pieces done.** Reused the existing `NewRelic-Integrations` app registration (not a first-time integration after all — see decision above); created `rg-ethico-sonarqube-mcp-dev`, granted it Reader on that RG, generated a new client secret, and configured a New Relic Azure account entry for this subscription with Azure Monitor metrics enabled. **Remaining:** confirm real Container App metrics actually land in New Relic once the Container App exists (can't verify before then) | 1 day |
| 7 | Azure DevOps Pipeline, authored in **this repo at `Pipelines/SonarQubeMCP/`** (corrected location — see decision above; new YAML file, name matching the ADO pipeline name): create an ARM service connection scoped to the target resource group (no approval needed — Walter has org owner rights); author a pipeline that pulls a new SonarQube MCP version tag → Trivy scan (no existing container-scan template to reuse — `devops` repo's `Vulnerability_Scan/templates/` is NuGet/npm-only, so this is new pipeline capability, possibly contributed back there if it proves reusable) → push to ACR (digest-pinned) → redeploy the Container App by calling the existing `devops/Azure/UpdateAzureContainer_WithEnv.ps1` script (requires the build stage to emit an `appsettings_envmapping.json` artifact for the container's env vars) → hits `/health` and `/info` as a verification gate → notifies on success/failure. This replaces the manual steps in [docs/mcp-maintenance.md](/docs/mcp-maintenance.md)'s Standard/Emergency Update Processes | 1.5–2 days |
| 8 | Registry & doc updates: replace the `stdio`/docker package block in [v0.1/servers/index.json](/v0.1/servers/index.json) and [v0.1/servers/mcp/sonarqube/versions/latest/index.json](/v0.1/servers/mcp/sonarqube/versions/latest/index.json) with `executionModel: "remote-http"` + `remotes: [{transportType: "http", url: "https://<container-app-fqdn>/mcp"}]`; replace the `<container-app-internal-url>` placeholders in [README.md](/README.md) and [docs/sonarqube-deployment.md](/docs/sonarqube-deployment.md) with the real FQDN; update [docs/mcp-maintenance.md](/docs/mcp-maintenance.md)'s inventory table and update process to reference ACR + the new pipeline instead of manual CLI steps | 0.5 day |
| 9 | Developer-facing token documentation in the README's SonarQube section: step-by-step for generating a SonarQube **USER token** (sign in to `https://sqdev.mycompliancemanagement.com` → *My Account → Security → Generate Tokens* → type **User**, not Project/Global Admin, named `mcp-<username>`), where to paste it (the `Authorization: Bearer <token>` header in their own `mcp.json`, per the existing example block), a reminder tokens are personal/not to be shared, and a note on what breaks when a token expires (a 401 from the MCP) and how to generate a replacement | 0.5 day |
| 10 | Token expiry governance: set the global max token lifetime policy in SonarQube (*Administration → Configuration → Security*); build a scheduled ADO pipeline, also under **this repo's `Pipelines/SonarQubeMCP/`** (reusing the service connection from #7), that calls the SonarQube Web API with an admin token (stored as a pipeline secret) to find `mcp-*` tokens nearing expiration and emails each owner a reminder ahead of time — adapt `devops` repo's `Vulnerability_Scan/sendemail_scan.ps1`'s SES/SMTP-via-`SmtpClient` pattern for per-developer sends rather than the fixed-list Action Group email used for Container App alerts | 1 day |
| 11 | Verification (see below) | 0.5 day |
| 12 | Pilot rollout: share config with 1-2 volunteers, observe for ~1 week, then update the GitHub issue/#134516 and announce org-wide | 0.5 day active + ~1 week passive observation |

**Total active work: ~8–9 days**, spread over roughly two to three weeks — the spread is driven by the pilot observation window (~1 week passive), not by approval or service-connection lead time, since Walter holds org owner rights and can provision those himself.

## Suggested Sequencing — Revised 2026-08-11

**The original Friday Aug 7 target passed without the work being picked back up** — the gap between the plan being written (2026-08-02/03) and this revision was 8 calendar days of no activity on the project, not a schedule overrun on work actually in progress. Re-baselining from today (Tuesday, 2026-08-11) rather than pretending the original week still holds.

**Actual state today:** #1 done. #2 approved by Gilbert but the subnet isn't created yet. #3's Bicep is authored (ACR + managed identity + role assignment modules, compiles clean) but not deployed, and the image hasn't been pulled/scanned/pushed. #6's front-loadable pieces (RG, Reader role, New Relic account entry) are done; its metrics-verification step still waits on #4. #4, #5, #7, #8, #9, #11 haven't been started.

Remaining estimated effort: #2 (~30 min) + #3 (~0.25 day) + #4 (1–1.5 days) + #5 (0.5 day) + #6 close-out (~0.1 day) + #7 (1.5–2 days) + #8 (0.5 day) + #9 (0.5 day) + #11 (0.5 day) ≈ **5–6 days**, against the dependency chain #2 → #3 → #4 → #5/#7 (#6's remaining piece rides along with #4).

New target: **tasks #1–9 and #11 done and verified by end of day Tuesday, Aug 18** (5 business days from tomorrow), with realistic risk of slipping to **Wednesday, Aug 19**. That risk is concentrated in **#7** — first-of-its-kind Trivy pipeline step and first real use of `UpdateAzureContainer_WithEnv.ps1` for this env-var set — the same risk the original plan flagged and front-loaded #6 to hedge against; #6 no longer needs hedging (its unfamiliar piece turned out to already exist and just needed reuse), but #7's risk hasn't been retired by anything that's happened since.

| Day | Focus | Notes |
| --- | --- | --- |
| **Tue (today, remainder)** | Finish #2 (create the subnet, NSG + route-table association) → finish #3 (deploy Bicep via `az deployment sub create`, pull/Trivy-scan/push the image) | Both are mostly execution at this point, not design — the risk here is low |
| **Wed** | #4 (Container Apps Environment + Container App + Log Analytics, extending `infrastructure/SonarQubeMCP/main.bicep`) → close out #6 (confirm New Relic reflects real Container App metrics) | Pivot day — the Container App goes live. If #4 overruns into Thursday morning, that's the first place to watch, same as the original plan |
| **Thu** | #5 (Azure Monitor alert rules + Action Group) → start **#7 pipeline** | |
| **Fri** | Continue #7 (Trivy scan step, ACR push, `UpdateAzureContainer_WithEnv.ps1` wiring, health-check gate) | Biggest remaining risk, same as originally flagged |
| **Mon (Aug 17)** | Finish #7 → #8 (registry/doc updates) | |
| **Tue (Aug 18)** | #9 (developer token docs) → #11 (verification checklist) | If #7 ran long, let #9's polish slip a little rather than #11's verification — same fallback logic as the original plan |

Task #10 (token-expiry governance) remains independent and unscheduled — slot in whenever. Task #12 (pilot rollout) starts once #1–9/#11 land (~Aug 18–19), plus its ~1 week passive observation window, putting full close-out around **~Aug 25–26**.

## Verification Steps

1. **Image provenance** — confirm the running Container App revision references the ACR image (by digest), not Docker Hub directly; confirm the Trivy scan report for that digest shows no unaddressed High/Critical findings.
2. **Infra reachability** — from a VPN-connected machine: `curl https://<container-app-fqdn>/health` returns HTTP 200 with an empty body; `curl https://<container-app-fqdn>/info` returns `{"version":"1.24.0"}` (updated from `1.20.0` — see task #3's version-bump decision).
3. **Ingress isolation** — from a network NOT on VPN (e.g. phone hotspot), confirm the same URL fails to resolve/connect, proving internal-only ingress is actually enforced.
4. **Alerting fires** — deliberately stop the container app (or push a bad revision) briefly and confirm the health-check alert fires and the notification email arrives; confirm New Relic also reflects the outage.
5. **Pipeline dry run** — trigger the ADO pipeline against a no-op version bump (or a test tag) and confirm it completes pull → scan → promote → deploy → verify end-to-end without manual intervention.
6. **Functional / end-to-end** — from VS Code, point `mcp.json` at the real URL with your own real SonarQube USER token (per [README.md](/README.md)) and ask Copilot Chat to list open issues for a real project on `https://sqdev.mycompliancemanagement.com`; confirm results return.
7. **Read-only + token governance enforcement** — attempt a write-style action (e.g. resolving an issue) through the MCP and confirm it's rejected (`SONARQUBE_READ_ONLY=true`); confirm a SonarQube Project token or Global Administrator token is rejected (only USER tokens should work); confirm the request shows up in SonarQube's audit log tied to your specific token.
8. **Registry correctness** — after task #8, validate both JSON files parse and that VS Code's `@mcp` picker shows SonarQube as a remote-HTTP server (not attempting a local `docker run`).
9. **Token docs are actually followable** — have one of the pilot volunteers (someone who hasn't seen the internal design docs) follow only the README's token-generation section cold, with no extra help, and confirm they can generate their own USER token and get a working connection from it.
10. **Token expiry reminders work** — generate a test `mcp-*` token with a near-term expiration date, run the scheduled pipeline manually, and confirm the reminder email arrives for that token before it expires.
